# Frontend environment (optional for local dev)
#
# The API key is NOT set here. Live football data is fetched by
# the Backend using API_KEY in Backend/.env — each user adds
# their own key after download/install.
#
# Copy to .env only if you change the backend URL.

VITE_API_URL=http://localhost:5003
